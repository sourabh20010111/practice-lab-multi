**CSS Best Practices** so your styling stays clean, maintainable, and professional.

---

## 📌 1. Naming Conventions

* **Use clear, meaningful names** — avoid `div1`, `box2`
* **Lowercase + hyphens** for multi-word names → `main-header`, `btn-primary`
* If project is big → consider **BEM (Block Element Modifier)**
  Example:

  ```css
  /* Block */
  .card {}
  /* Element */
  .card__title {}
  /* Modifier */
  .card--highlight {}
  ```

---

## 📌 2. File Structure

```
project/
│── index.html
│── css/
│   ├── style.css
│   ├── reset.css  /* Optional: Reset browser default styles */
│── images/
│── js/
│   ├── script.js
```

---

## 📌 3. Keep CSS DRY (Don’t Repeat Yourself)

* Use **CSS Variables** for repeating values
* Use **shared classes** instead of duplicating rules
* Example:

  ```css
  .btn {
    padding: 10px 20px;
    border-radius: 5px;
  }
  .btn-primary { background: blue; }
  .btn-secondary { background: gray; }
  ```

---

## 📌 4. Organize Your CSS

* **Top to bottom order**:

  1. Reset / Normalize styles
  2. Variables
  3. Base styles (body, headings, links)
  4. Layout (grid, flex containers)
  5. Components (buttons, cards)
  6. Utilities (small helper classes)

---

## 📌 5. Performance Tips

* Combine and minify CSS in production
* Use shorthand properties where possible (`margin: 10px 20px;`)
* Avoid deep selectors like `.header nav ul li a {}` — keep it short
* Use **`will-change`**, `transform`, and `opacity` for smoother animations

---

Now that CSS is **fully covered from basics to advanced**